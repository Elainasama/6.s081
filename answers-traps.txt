1.a0、a1、a2寄存器存放函数的参数，13存放在a2寄存器
2.在printf中调用，汇编中直接返回了结果，因此可能直接做了函数内联。
3.0x642
4.当使用 jal 或 jalr 指令进行函数调用时，当前指令的地址（PC+4）会被存储到 ra 寄存器中。
这样，在函数执行完毕后，可以通过将 ra 寄存器中的值加载到 PC 中，从而返回到调用点继续执行。
printf返回地址为0x38。
5.代码输出HE110 World，57616 = 0x00e110,如果是大端序列则i需要改成0x726c6400，57616无需改变，从高位读起。
6.‘y=’之后会输出一个随机值，这是由于未提供实参给printf，故printf从寄存器a2中获取的是一个垃圾值。