可以将一些系统常用的参数都放入只读区域中共享数据，也可以加速sys_read。将要读取的部分存入一个区域，并将用户访问位和只读位置为1，类似getpid的实现。

（三级页表控制）第零页包含第一页页表的物理地址信息和一些控制位，第二页包含真实物理地址的页表PTE(包含一些控制位)。
处于用户态不可读写第一页映射的内存，其用户控制位不为1。
倒数第三页是最后一级页表，通过va的低12位偏移可以找到真实的物理地址。
